JuggyWishlist_Data = {
  <%- @wishlists.each do |wishlist| -%>
  ["<%= wishlist.item.wow_id %>"] = "<%= [escape_javascript(wishlist.priority.titlecase), escape_javascript(wishlist.note)].join(' - ').gsub(/ - $/, '') %>",
  <%- end -%>
}