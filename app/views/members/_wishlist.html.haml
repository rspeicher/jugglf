%table.list
  %thead
    %tr
      %th Item
      %th.note Note
      %th Type
  %tbody
    %tr{ :class => cycle('even', 'odd') }
      %td= link_to_item_with_stats(wishlist.item)
      %td= h(wishlist.note)
      %td= h(wishlist.priority.titlecase)