<%- if admin? -%>
JuggyCompare_Data = {
  ["updated"] = "<%= Time.now.to_s(:long) %>",

  <%- for member in @members -%>
  ["<%= member.name %>"] = {
    ["NAME"]     = "<%= h(member.name) %>",
    ["RANK"]     = "<%= h(member.rank.name) unless member.rank_id.nil? %>",
    ["RAIDS_30"] = "<%= (member.attendance_30 * 100.0).floor %>%",
    ["RAIDS_90"] = "<%= (member.attendance_90 * 100.0).floor %>%",
    ["RAIDS_LT"] = "<%= (member.attendance_lifetime * 100.0).floor %>%",
    ["LASTLOOT"] = "<%= member.last_loot.purchased_on %>",
    ["LF"]       = "<%= loot_factor(member.lf) %>",
    ["SLF"]      = "<%= loot_factor(member.sitlf) %>",
    ["BISLF"]    = "<%= loot_factor(member.bislf) %>",
  },
  <%- end -%>
}
<%- end -%>