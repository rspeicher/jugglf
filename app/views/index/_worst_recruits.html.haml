%h4 Least Recruitable Class
%table.list
  %thead
    %tr
      %th Class
      %th.number Declined
  %tbody
    - worst_recruits.each do |wow_class, percent|
      %tr{ :class => cycle('even', 'odd') }
        %td{ :class => wow_class }= wow_class.titlecase
        %td.number== #{(percent * 100).floor}%