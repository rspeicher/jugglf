# Show the errors div
page << "$('#errors').removeClass('hidden');"

# Add the error messages
page << "$('#errors').html('#{error_messages_for('wishlist entry', :object => create_failure)}')"

# Focus the name field
page << "$('#wishlist_item_name').focus();"