require 'spec_helper'

describe Attendance::LootController do

  #Delete this example and add some real ones
  it "should use Attendance::LootController" do
    controller.should be_an_instance_of(Attendance::LootController)
  end

end
